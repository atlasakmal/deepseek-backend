services:
  - type: web
    name: deepseek-chat-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: NVAPI_KEY
        value: your-nvapi-key-here